import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    input: {
        height: 48,
        width: "100%",
        fontSize: 16,
        borderColor: "#dcdcdc",
        borderWidth: 1,
        borderRadius: 8,
        paddingLeft: 12,
    },

});

export default styles;